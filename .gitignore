# Node
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Builds
dist/
build/
.next/
coverage/
*.tsbuildinfo

# Env — never commit secrets even in an OSS repo (test keys, local dev vars)
.env
.env.local
.env.*.local
!.env.example

# Editor
.DS_Store
.idea/
.vscode/
*.swp

# Lockfile policy:
#   Keep pnpm-lock.yaml / package-lock.json at the root so contributors get
#   reproducible installs. Nothing to ignore here; this block is a reminder.

# Nothing below this line unless explicitly reviewed.
